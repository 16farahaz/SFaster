<form method="post" action="{{ path('qualite_m_delete', {'id': qualite_m.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ qualite_m.id) }}">
    <button class="btn">Delete</button>
</form>
