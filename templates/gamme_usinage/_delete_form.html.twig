<form method="post" action="{{ path('gamme_usinage_delete', {'id': gamme_usinage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gamme_usinage.id) }}">
    <button class="btn">Delete</button>
</form>
